# Copy to .env.local and adjust. VITE_* values are baked into the built
# JavaScript, so never put secrets here.

# Base URL of the Lead Tracker API (no trailing slash).
VITE_API_URL=http://localhost:3000
